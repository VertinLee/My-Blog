# 语言包目录：禁止任何 HTTP 直接访问（内核经 include 加载；
# 本文件为双重防护之一，另一重为包文件头部 defined('APP_BOOT') 守卫）
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
